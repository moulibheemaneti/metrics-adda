# ads.txt — authorised digital sellers (IAB Tech Lab spec).
#
# Declares who may sell this site's inventory. Without it AdSense marks the
# domain as unauthorised and demand drops sharply, so this file has to be
# live before the ad unit earns anything.
#
# Unlike the publisher ID in `runtimeConfig`, this cannot come from an env
# var: crawlers fetch a static /ads.txt, so the ID is literal here. Replace
# the placeholder below with the pub- ID from your AdSense account (the
# digits only — no "ca-" prefix) before deploying.
#
# Verify after deploy:  curl https://www.metricsadda.com/ads.txt
google.com, pub-0000000000000000, DIRECT, f08c47fec0942fa0

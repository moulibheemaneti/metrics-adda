commitguard:
   verbose: true

pre-commit:
   commands:
      eslint:
         run: bun eslint .
      stylelint:
         run: bun stylelint '**/*.scss'
      typecheck:
         run: bun typecheck

commit-msg:
   commands:
      conventional:
         preset: conventional
